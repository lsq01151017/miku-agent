# 修改记录 —— 每一轮要什么、改了什么、为什么、怎么证明

每一轮一段，按时间先后。改动本身的原始出处是 `D:\二面\cortico` 的 git 历史（提交号写在段末），
这份文件写的是历史里看不到的部分：**为什么这么改**，以及**怎么证实改对了**。
版本一（`my-agent/`）不是 git 仓库，只有一段概述。

---

## 0. 版本一：Python 单体（先做出来的那套）

- **要什么**：题目 Lv1（持续状态 / 人格 / 记忆 / 行动）到 Lv2（记忆生命周期），并带一个能看的形象。
- **是什么**：`router.py` 判断这一轮该"回答"还是该"行动"；`local_agent.py`（Ollama `deepseek-r1:14b`）
  出人格、语气、情绪与记忆注入；`api_agent.py`（DeepSeek `deepseek-flash`）出工具调用与真实行动；
  `memory.py`（SQLite）管存储 / 检索 / 覆盖修正 / 遗忘 / 衰减 / 巩固；`state.json` 存情绪；
  `live2d/avatar_server.py` 自起形象服务（端口 8765）。
- **怎么验证**：脚本化验收用例 + `logs/agent_log.jsonl` 每轮一个 `trace_id`。
- **局限（如实）**：单体、没有扩展点；加一个渠道或换一个模型都要动那几个 20–56 KB 的 `.py`；
  人格与协议细节混在同一个文件里。取舍细节见 `my-agent/README.md`。

## 1. 定底座：把 Cortico 拿下来当版本二（c8c06b4）

- **要什么**：以 Lv3（表现力）为主，但基础框架用它的，保持人家原有的扩展性与优势；能直接用的就用，
  不怕改核心；记忆方面着重学它的做法；与题目要求冲突时以题目为准；不要它自带的直播与 QQ 那几路。
- **改了什么**：把 Cortico 上游（MIT）vendor 进 `cortico/`，记下版本。
- **为什么**：题目要的是"持续状态 / 人格 / 记忆 / 行动"的智能体，Lv3 要表现力。Cortico 已经把
  Core（语义无关的内核）/ Persona / Memory / World / Bot 分开，表现力正好能做成一个 World 扩展包：
  加渠道、换模型、调人格都不动别人的代码。
- **怎么验证**：vendor 之后 `pnpm test` 与 `pnpm run typecheck` 直接绿。

## 2. 人格包：先让她"是她"（e7a6029）

- **改了什么**：`bots/miku` 继承 Cormini（工作区记忆、Git 记账、交接、心跳、工具面都来自基类），
  加角色宪法、带 EMOTION 段的前缀模板、六维情绪状态（存在 `CoreApi.personaState()`）。
- **为什么**：只有**离散心情**进系统前缀——连续六维每轮都在变，会把前缀复用切碎，而心情很少换档；
  词典是"这句话对说话人意味着什么"的启发式推断，不是系统能确认的事实，所以做成可关的开关
  （`emotion.enabled` 关掉后状态冻结，前缀照常渲染）。
- **怎么验证**：情绪衰减半衰期的断言——第一版把 `decayScale` 乘反了，是断言抓住的。

## 3. 接入本地模型：Ollama provider 与文本端点的工具协议（3befe8b / 26a7baa / a3af296 / 96844a3）

- **要什么**：本机 Ollama `deepseek-r1:14b` 也要能跑。
- **改了什么**：① `cortico-provider-ollama`（把 `reasoning` 改名成 `reasoning_content`，复用框架
  自己的 transport；窗口没实现，因为 Ollama 不报运行时窗口，拿不到的字段就是死代码）；
  ② 文本端点的工具协议 `src/tool-protocol.ts`：Chat 模板缺 `.Tools` 的模型收不到请求体的 `tools`，
  它会把调用写成正文，端点上把围栏块翻成一次调用；③ 打开 `toolProtocol.enabled` 后把工具表写进
  前缀最后一段；④ 工具表瘦身成"每个工具一句话"（前缀 6846 → 4764 字符）。
- **为什么**：模型属于端点条目，能不能收结构化声明是传输层能确认的事实，提示词只负责教学；
  工具表取自 `declareSessions()` 而不是另拼一份清单——前缀里写错工具名等于教模型调一个不存在的工具。
  工具表每轮随前缀重发，而描述本来是写给"能收到结构化声明"的模型的，所以只取第一句。
- **怎么验证**：协议在流式与非流式两条路径上的用例；断句要求句点后跟空白或行尾，否则 `[blob ...]`
  里的省略号会把句子切成半截。

## 4. 记忆的机械部分：三层、容量门、写矩阵、当场忘记（4283196 / 8ba333a / ef3a4a7）

- **要什么**：Lv2 的记忆生命周期要真的成立，不是靠提示词自觉。
- **改了什么**：`memo/` 三层（常驻层全文进前缀 / `active/` 只列名 / `archived/` 只报条数，容量 7 / 21，
  满了不自动下沉，回执报当前占用与成员名单并指向 `move_file`）；写矩阵（主意识可写可改可搬但不能删，
  `people/` 只许追加，宪法清醒时只读）；`forget` 工具当场删文件**并请求重建系统前缀**；
  人物档案的概括改成取第一条正文而不是第一行标题。
- **为什么**：常驻内容住在系统前缀里，而前缀只在会话建立、交接与清空时重建——删了文件也照样发，
  所以"明确要求忘记"必须开一个重建入口给 Persona；容量是机械规则，所以硬拦。
  真实模型写档案时先写 `# 名字` 再写正文，抽取器只看第一行就只拿到标题，所以是机械层迁就它的写法。
- **怎么验证**：端到端——第一批含秘密、最后一批不含（前缀 5346 → 5316 字符）；真实模型在两轮内写下
  `people/制作人.md`，下一轮答出了"秋天"。

## 5. 梦：交接后整理记忆（19b2666 / 30e0014）

- **改了什么**：一次交接一场梦（串行队列，并发写工作区会互相踩），从交接前的快照 fork，
  结束时的 `surface` 回到醒来那一侧；修 `nudge` 的条件（整轮都在调工具、一个字没写也要拿到收尾机会），
  并把"该收尾了"的引导写进提示词。
- **为什么**：本地窗口只有 4096 token，引导要紧凑版，留给动态尾的余量按预算比例算（写死 8000 会在
  3072 的预算上把动态尾挤成零）；梦排在 `super.onHandoff` 之后，交接笔记要先落盘它才读得到。
- **怎么验证**：实测 12 场梦里浮现 6 → 7，撞硬上限归零。

## 6. 表现力地基：把素材包读成通道值（21c13a1）

- **改了什么**：三层合成——情绪算出的**基线**一直在，词表命中的**片段**叠上去，最后按量程裁剪；
  片段三种形状都支持（关键帧 `pulse`、保持值 `sustain`、视线 `gaze`）；噪声与扫视是时间的纯函数
  （正弦，不是随机数）；长词优先、别名归一；包里提到的片段缺失时列进 `missingClipIds` 而不是静默跳过。
- **为什么**：题目硬要求"身体由内部状态驱动"，基线就是那一层；静默跳过会让"特效为什么不出现"变成谜。

## 7. 形象 World：自己起渲染服务，把通道推给页面（75d92f8 / 840800b / 31c6f5c / a77d84a / a0cc00b / 81f71ea）

- **改了什么**：`live2d` World 自起 HTTP 服务（只绑 127.0.0.1，`/state` 用 SSE 推通道值，只在内容变了
  或正在说话时播发）；`outputTap` 收正文增量走词表触发片段；`externalizes` 只在**真有页面看着**
  且这一轮确实说了字时为真；通道按模型自己的参数表核对；表情由离散心情驱动；随包资源（`web/`、
  `ENV_PROMPT.md`）列进 `package.json` 的 `files`；页面测试用替身浏览器跑页面自己的逻辑。
- **为什么**：不碰控制台前端——改它要重建 Web 产物，而重建必须在没有 bot 活着的时候做。
  通道的 `suggests` 只是建议：这份模型上有 `Paramguzui` 这种拼音命名，包里建议的 `ParamCheekPuff`
  根本不存在，那一路表演此前是静默失效的，所以拿模型的 `DisplayInfo`（cdi3）核对并把对不上的通道
  连同"丢了什么"一起报出来。配置问题在挂载时就报，不留到有人打开页面才发现。
- **怎么验证**：真实包 + 真实模型的参数表，唯一对不上的就是腮那一条，这条进了测试。

## 8. 睁开眼、关掉水印（2adf62a / 1264ea1 / ec3999a）

- **要什么**：水印没关掉。
- **根因**：不是配置错，是**写的时机**——Cubism 每帧把参数复位成模型默认值，复位发生在 pixi 自己的
  更新里；在自己的 rAF 里写和它的顺序不定，写早了当帧就被抹掉。另外部署里钉的是 `Param137=0`，
  而那正是模型默认值、也就是"水印开着"的一档。
- **改了什么**：参数覆盖改到模型的 `beforeModelUpdate`（复位之后、更新之前）；部署钉 `Param137=1`。
- **怎么验证**：用真的 Cubism core 驱动模型文件实测——`Param137=1` 时三层水印（ArtMesh58 / 59 / 28）
  的不透明度从 1 变 0。模型文件没有改动；测试只覆盖管线（真模型不是仓库夹具）。

## 9. 她说的话从哪来：工具参数里的台词，措辞就是演出指令（962d772 / 189ff5a）

- **要什么**：脸上一直没有表情变化。
- **根因**：① 她的台词是**带 `speak` 标签的工具的参数**说出来的，正文一个字符都不在
  `response.output_text.delta` 里，挂在输出上的 World 只能收到参数增量，所以形象层一直以为她没说话；
  ② 表情层只认离散心情，而心情不常换档。
- **改了什么**：覆写输出旁路，按说话工具的 `parameters` 找到装话的那个字符串字段，把参数流翻成正文流
  再交给各 World（字段名从工具声明里读，不写死 `text`）；素材包新增 `expressions.json`
  （`{ expression, words[] }`），正文命中就挂模型自带的那个表情，压过心情那张，`expressionHoldMs`
  到期回到心情；帧里多一个 `expressionToken`（名字没变也要能重放同一张表情）；说话时长改为按字数估。
- **为什么**：参数是流式拼出来的，`partialStringArgument` 在结尾的 `\` 与 `\uXXXX` 到齐前不吐字；
  她的话算外部输出，这一轮不该被新输入抢占。
- **怎么验证**：真实一轮里命中表情与片段；同一段正文按不同位置切分得到同一串文本。

## 10. 删掉四个不用的渠道（94bcb5c / 04a0573 / 29d3f81）

- **要什么**：不要它的直播和 QQ 那几路，专注 AI 人格。
- **改了什么**：删 QQ / B站直播 / Minecraft / 联网搜索四个 World 与只服务它们的代码、依赖
  （占地最多的是 mineflayer 那棵树）、假数据控制台 `dev-console`；两个示例 bot 的提示词、目录约定
  与文档同步。仓内只剩 `terminal` 与 `console-fixture`。
- **为什么**：渠道是扩展点，不是内核功能；留着一万九千行不用的源码与依赖只是负担。
- **怎么验证**：契约测试收敛到剩下的 World，`pnpm typecheck` 与测试绿。

## 11. 她自己会动起来、形象页有数可取景（fde0010）

- **要什么**：不管说什么都像只有一个动作；画面上看不出任何数值；不能调她的位置。
- **改了什么**：**待机层**——呼吸、微晃、视线游移，几个不同周期的正弦，一直在（`idleAmount` 可关）；
  **说话自带动作**——每句起音一次 `speech_onset`，之后每说满 14 个字点一下头；推流帧带上面板要的
  `emotion` / `mood` / `clips`；右侧数值面板 + 取景（缩放 / 左右 / 上下 / 直接拖动 / 复位 / 记住）。
- **为什么**：以前只有情绪基线与词表片段，她不说不动的时候就是一张静止的图；取景只影响画面，
  不回写模型。
- **怎么验证**：待机随时间变且同刻同值、`idleAmount=0` 完全静止、起音与重音轮换不重复、
  页面上的条宽与数字来自帧、缩放/偏移/拖动/复位/记住都对。

## 12. 改完扩展源码要重装一次快照（4a9b5b2）

- **为什么**：启动器读的是 `extensions/node_modules/<包名>`，不是 `extensions/<包名>` 源码目录；
  `file:` 依赖会被缓存，不重装就跑上一次那份——形象页改了却没生效就是这个。
- **做法**：写进 `docs/development.md`；每次改完扩展源码重装一次快照（见第 27 轮：`pnpm install`
  本身不够，`file:` 依赖按名字缓存）。

## 13. 眼神自己算，腰不许跟着鼠标转（3cafd76）

- **要什么**：眼睛跟得慢；而且一拖鼠标她的腰跟着转。
- **根因**：播放器库自带的指针交互把指针位置映射成单位圆上的角度，速度上限约 0.6/s（整程约 1.6 秒才
  走完），并且写 `ParamBodyAngleX = 10x`。
- **改了什么**：关掉自带交互（`autoInteract: false`），自己按指针位置算眼神（时间常数 0.03s），
  一个身体参数都不写。
- **怎么验证**：推到最右 8 帧（≈134ms）眼睛偏移走过 0.6 以上且是叠加写；`ParamBodyAngle*` 一个都不出现；
  指针离开后回到 0.1 以内。

## 14. 对话框搬进形象页，页面与脚本不吃缓存（d681956 / 7b68be9）

- **改了什么**：形象页底部加输入区（走 `/chat` WebSocket，或用 `/agent/chat` 走外部 Agent）；
  页面与脚本的响应带 `no-store`。
- **为什么**：刷新后新旧脚本混在一起，谁也看不出改动有没有生效。

## 15. 她的话走字幕，输入区与按键换成磨砂玻璃（2cc86f8）

- **要什么**：她的回答做成浮在模型上方的字幕；底部只留我发出去的话；面板与按键统一磨砂玻璃。

## 16. 让她张嘴（a3ef03a）

- **要什么**：让她张嘴时没有对应动作。
- **根因**：词表里没有这组词，而这种片段要词汇命中才会触发；而且说话时口型通道被振荡器占着。
- **改了什么**：素材包加 `mouth_open` 片段（口型 0 → 0.95 → 0.9 → 0.35 → 0.85 → 0，约 1.6 秒）与
  「张嘴 / 张开嘴 / 张大嘴 / 张开嘴巴 / 啊——」等词；口型取"振荡器与通道值的较大者"，
  不然片段里的口型会被说话的振荡遮住；操作员在控制台打的话也递给表现层（以前只喂情绪那一路）。
- **怎么验证**：实测操作员发「张嘴」→ 活着的片段里有 `mouth_open`，`MouthOpen` 峰值 0.95，
  且峰值落在**不说话**的那些帧上。

## 17. 字幕更扁、输入区收起历史、口型听 World（2a56f59）

- **改了什么**：字幕不收边更扁；输入区只留最近一句、其余收起可展开；口型改由 World 的说话时长驱动。

## 18. 面板收起更多、取景不把她弄丢（88d7faa）

- **改了什么**：内部状态面板里数值之下的东西默认收起（「更多」展开，选择记在本地）；取景加边界，
  至少留 80px 的她还在画面里。
- **注意**：这一轮把"贴合窗口的比例"的计算挪进了 `applyTransform`，埋下了第 22 轮的 bug。

## 19. 字幕衬底回一版、比输入区略长（168b02e）

- **要什么**：拖动时出现两个模型；字幕衬底回去一版，宽度比对话框略长。
- **改了什么**：画布加 `draggable="false"`、`-webkit-user-drag` / `user-select` / `touch-action`，
  `pointerdown` 与 `dragstart` 都 `preventDefault`，`boot()` 加只进一次的守卫。
- **结果**：影子还在——真正的原因不在拖拽，见第 22 轮。

## 20. 换成一个透明覆层接指针（dd501ca）

- **改了什么**：画布上盖一层 `#hit`，指针事件全落在它身上，拖动取景的逻辑搬到覆层；文档级
  `dragstart` 拦截兜底；字幕行高 1.32 → 1.12；字幕与输入区放进同一个底部竖栏（输入区展开时字幕
  跟着上升，两者不再叠在一起）。
- **结果**：影子还在。这一步排除了"画布被浏览器拖走"这条路。

## 21. 字幕行高收到 1（c6e1d5b）

- **改了什么**：行高 1、上下内边距归零（字号不变）；流式吐字时行尾空白与连续空行不再占位。
- **为什么**：`white-space: pre-wrap` 会把行尾空格和空行原样排出来，看着就是凭空多出的行距。

## 22. 根因：贴合比例把缩放乘重了（2127633）

- **她看到的**：一拖动就有一层比原模型大得多的影子跟着鼠标，松手立刻消失。
- **根因**：模型在 PIXI 里是容器，`width` / `height` **含当前缩放**。第 18 轮把比例计算挪进
  `applyTransform` 之后，拖动每动一下就算一次，比例变成 `R / 当前缩放`，于是缩放按 `1 ↔ R` 反复
  取反：她在"贴合窗口"和"自然尺寸"之间来回跳。屏幕上就是一张大得多的她跟着鼠标，交替出现看着像
  两个模型——所以所有挡拖拽的办法都没用，它压根不是 DOM 拖拽。
- **改了什么**：加载完成、`scale` 还是 1 的那一刻量一次模型自己的尺寸，贴合比例与取景边界一律用它。
- **怎么验证**：页面测试的模型替身改成 `width` / `height` 随缩放变（这是它此前漏掉这个 bug 的原因），
  新增断言"连续拖动两次缩放不变"；把旧算法放回去跑，这条断言立刻失败（期望 9、实为 1）；
  换回新算法，扩展与页面的 85 项测试全过、`tsc --noEmit` 干净。

## 23. 归档记录

- **要什么**：把所有记录类文件放到一个文件夹，再多一份逐轮修改记录与作用。
- **改了什么**：新建 `D:\二面\记录\`；根目录的 `README.md`、`deepseek_share_*.md/json` 与
  `cortico/scratch/ACCEPTANCE.md`（改名 `验收证据与取舍.md`）搬进去；新增本文件。
- **没搬什么、为什么**：`my-agent/README.md` 是版本一自己的说明，`cortico/**` 的文档是框架自己的
  影子文档（AGENTS.md §8 要求它们跟自己的代码放在一起），`模型/miku/.../模型使用说明.txt` 随模型走，
  启停脚本与 `env-cortico.ps1` 是运行入口——搬了会影响项目功能。
- **顺带说明**：根目录还留着 `app.js`（0 字节）与 `debug.log`（一行 Edge 崩溃转储日志），都是误产生的
  文件，不是记录，没有搬。

## 24. 删冗余，治两处慢（6fd52b7 / f537080）

- **要什么**：打开时太卡——形象页慢、`启动她.bat` 启动慢；在不改变现有功能与速度的前提下找出
  冗余并删除，删除前先问。
- **删了什么**：根目录误产生的 `app.js`（0 字节）与 `debug.log`（一行 Edge 崩溃日志）；
  `模型/miku/miku/`（34.6 MB，与 `my-agent` 里那份模型完全重复）；形象页左上角的**临时拖动探针**
  （第 22 轮为验证影子消失加的调试读数，连同 `bindDragProbe`、`#probe` 样式与那行 HTML，是
  `6fd52b7`）。前两样在 `D:\二面` 根目录，不在任何 git 仓库里。
- **没删什么、为什么**：`新建文件夹/`（47.6 MB，操作员选了保留，版本一的备份）；素材包里十个
  `fx_*` 词条（`performance.test.ts` 断言包里缺失的片段全是 `fx_*`，删了测试就红）；
  `miku.4096/` 原图（降分辨率后留作回退）。
- **启动慢的根因（实测）**：这台机器上一个**关闭的回环端口要约 2 秒才拒绝连接**——裸 `TcpClient`
  同步连接实测 2.03–2.06 秒，所以不是 `Invoke-WebRequest` 的问题，换任何 HTTP 探活都一样。
  启动脚本预检付一次约 2 秒，等待循环每轮再付一次（1 秒睡眠 + 2 秒探活 = 3 秒一轮）；另有
  `corepack pnpm start` 这层包装约 0.75 秒。
- **改了什么**：`start-miku.ps1` 新增 `Test-Port`（TCP 连接、400 ms 预算：活端口 0.00 秒接受，
  死端口 0.45 秒放弃），三处探活全换；启动命令改 `node bin\cortico.mjs miku`——`package.json`
  的 `start` 就是它，bin 自己会在缺依赖时跑 `pnpm install`。脚本自己的开销从约 3 秒压到约 0.6 秒。
- **形象页慢的根因（实测）**：6 张 4096² 贴图占 25.4 MB 传输、384 MB 显存、约 170–190 ms 解码，
  而默认缩放下模型只渲染 425 px 宽（贴图被缩小 9.6 倍）；`sendFile` 又对每个文件发 `no-store`，
  重复打开全量重传。
- **改了什么**：生成 `miku.2048/`（2048²，共 6.94 MB，-73%），`miku.model3.json` 改指新目录，
  4096 原图保留可回退；`world.ts` 的 `sendFile` 改 ETag（尺寸+修改时间）复验——未改动的文件回
  304 零正文，改动的换 ETag 照发全文，"改完源码刷新一次就是新的"不变（`f537080`，附
  200→304→200 契约测试）。
- **被实测否掉的方案**：`bin/cortico.mjs` 的 `--list` 短路（`--json` 与 `--list` 热缓存下只差
  约 0.1 秒，早前量到的 3.58 秒是冷缓存一次性数字）；`-NoProxy` 探活改法（Windows PowerShell
  5.1 没有这个参数，`try/catch` 把报错吞掉，探活会静默失效）。
- **怎么验证**：`tsc --noEmit` 绿；扩展 86 项测试 85 过 1 挂——挂的 `page.test.ts`
  （`ParamMouthOpenY` 断言）是**既有问题**：把 HEAD 版 `app.js` 换进去重跑同样挂，它也不读
  `index.html`、不 import `world.ts`，另有 8 个既有类型错误（`FakeNode.value`），未修。
  探活函数单独实测（活 0.00 秒、死 0.45 秒），脚本过语法检查；完整启动按仓库规矩没有真跑
  （不许为验证起真 bot），等操作员下次双击实测。

## 25. 换端点:第三方 Chat 兼容服务与新 provider 扩展（1552b12）

- **要什么**：把项目引用的 API key 换成新 key。
- **查明的事实（实测）**：新 key 属于第三方兼容端点 `https://r.dis.115.zone/v1`——在官方
  `api.deepseek.com` 上它是 401。该端点 8 个模型全部只声明 `["openai"]`（chat/completions），
  `/responses` 一律 503；原生 `tool_calls`、流式、`reasoning_content`、末帧 usage 都正常。
  旧配置的 `deepseek-flash` 不在它的模型表里。
- **改了什么**：① 新扩展包 `cortico-provider-chat`：与 `openai-responses-compat` 按
  `/chat/completions` 与 `/responses` 分工——同一个端点通常只实现一套请求体。被否掉的替代：
  给 responses-compat 设 `endpointPath` 只换路径不换请求体，不会工作。②
  `extensions/pnpm-workspace.yaml`：没有它 pnpm 向上找到仓库根的 pnpm-workspace.yaml，
  把 `cortico/` 当安装根，`extensions/` 的依赖永远 "Already up to date"；此前两个扩展的
  junction 已在盘上，这次新增第三个依赖才暴露这个坑。③
  `deployments/providers/deepseek/config.json`：`kind: chat`、baseUrl 指新端点、模型
  `deepseek-v4.1-flash`；`contextWindow` 不填——端点不报窗口，猜一个填上去反而让 Core 按
  错值裁剪上下文。④ `my-agent/.env`：key/baseURL/model 三行；版本一直接读它，版本二的
  启动脚本也从它读进环境变量，改一处两个版本同时生效。
- **怎么验证**：扩展 6 项契约测试绿、`pnpm check:extension` 通过；走真实的
  `loadExtensions → registerProviderModules → registry.resolve` 全链路发真实生成（流式出字、
  usage 含 reasoning_tokens）；版本一用改后的 .env 真实调用 200。
- **顺带查明（与本轮无关的既有问题）**：根测试套件在本机 53 个文件收集失败——
  `src/providers/registry.ts` 顶层用 `pathToFileURL().href` 动态 import，vite 不解百分号编码，
  仓库路径 `D:\二面` 含中文就炸；最小复现是一个只 import `src/core/util.ts` 的空测试。
  CI 在 ASCII 路径跑同一套件是绿的；同一代码在 tsx（真实运行时）下也正常——bot 不受影响，
  只有 vitest 受影响。`tests/web/theme-handoff.test.ts` 另有一项引用第 10 轮已删除的
  bilibili editor.js，是陈旧测试。

## 26. 版本三·第一轮:记忆与性格的时间感（c9502bd）

- **要什么**：改记忆公式，补上记忆生命周期里最弱的"衰减"与"巩固"，同时让性格更人性化。
  从本轮起，这套 cortico 实现的继续演进叫**版本三**——不是另一套实现，代码与入口都不变。
- **被否掉的方案**：照搬 AAAAGENT 的召回公式 `P = C×(0.55+0.25A+0.15I+0.05E)`。它的 `C`
  （相关性）依赖当前输入、每轮计算，而我们的记忆是**常驻前缀、低频重建**——硬套会让前缀
  每轮都变，打穿前缀缓存（`types.ts` 的警告原话：内容每次不同就每次打穿前缀缓存）。
- **改了什么（记忆）**：年纪按 **mtime** 算——重写一条就是巩固，mtime 随之刷新，零新格式、
  零新存储。半衰期随层走：常驻 90 天、active 30 天（AAAAGENT 的 `H = 30×(1+2I)` 映射到已有
  的分层上——层就是重要性档）。超过半衰期进"很久没动"名单。前缀里每条记忆带年纪注记
  （`「日程」(3天没动)`），名单单独一行；梦的引导把名单点为下沉首选。**只报事实，翻新还是
  沉下去由她判断**（PHILOSOPHY.md：主动性优先，机械层不替她做取舍）。
- **改了什么（性格）**：寂寞不再只由措辞驱动。距上次有人对她说话超过 8 小时，寂寞以 24 小时
  半衰期**向上**累积（上限 0.5，与回落同一个指数族）——离开约四天，心情自然翻成"寂寞"，
  她会表现出来。词表补了重逢的话：「回来」「想你」会安抚寂寞。其余维度照旧向基线回落。
- **怎么验证**：`tests/miku` 66 项全绿（新增 8 项：年纪计算、半衰期分档、沉寂名单、抬头
  注记、宽限期内不算、累积有上限、四天翻心情、重逢安抚）；`tsc --noEmit` 干净。

## 27. 版本三·第二轮:表情带伴随动作,裸参数名也核对（9b6cac9）

- **要什么**：把 miku-cure 那份每模型配置的纪律用起来——补上裸参数名的核对，并加一层手工映射，
  让常用表情更生动明显。
- **改了什么（裸参数核对）**：`paramOffset` / `paramOverrides` 的键此前从不对照模型参数表。
  `world.ts` 里 `modelParams` 就在手边（370 行拿到），371-372 行拆出来的键却直接放行——
  写错名字每帧静默无效：把 `Param137=1` 打成 `Param1137=1`，水印会悄悄回来，一句告警都没有。
  现在与通道落点过同一道核对（`unknownParamNames`），对不上就告警。这与第 7 轮给通道做的
  `verifyAgainstModel` 是同一条纪律，只是核对对象从通道落点换成了裸参数名。
- **改了什么（伴随动作）**：`expressions.json` 新增 `staging` 段——表情挂上的那一刻播一次它的
  伴随片段：脸红→鼓嘴、比心→雀跃、唱歌→摇摆、温柔→前倾、圈圈→四顾。**表情参数与通道参数
  不相交**（表情只写 `Param125`/`Param130-137`，通道写 `ParamAngle*`/`Mouth*`/`Eye*`），所以
  身体动作叠上去不打架。分类固定 `staging`，伴随片段之间互相顶替，不与词表的片段抢同类位。
  词触发与心情触发两条路都汇到 `syncExpression`，所以两边都带上了动作。
- **顺带写成契约**：每帧写入顺序——合成（基线+待机+片段叠加）→ 按量程裁剪 → 偏移（可越量程，
  它修的是约定差）→ 渲染端先写通道、再写眼神、最后写覆盖。后一步可以修前一步，反过来不行。
- **不抄什么**：miku-cure 的**每模型手写配置文件**这个形态没抄。它的 `tracking.face.parameters`
  是适配者手抄的；我们是**推导**——包给建议、部署可覆盖、建议落空时读模型作者自己写在
  `miku.vtube.json` 里的映射、再拿 cdi3 核对。手写一份等于在模型自己的导出旁边再立一个真相源，
  会漂移（他们的文档自己都在警告别把旧角色的水印控制照搬到新模型）。抄的是纪律，不是形态。
- **怎么验证**：扩展 90 项测试绿（新增 5 项：裸参数名核对 2 项、`staging` 解析 2 项、伴随片段
  在表情换挡时播出且新表情顶掉旧的 1 项）；`tsc --noEmit` 干净。`page.test.ts` 那一项失败与
  8 个类型错误在本轮之前就有（见第 24 轮）。
- **推翻了第 12 轮的一条说法**：那里写"改完扩展源码跑一次 `pnpm install` 刷新快照"——**不成立**。
  pnpm 对 `file:` 依赖按**名字**缓存，源码改了但版本号没变，`install` 与 `install --force` 都
  不会重拷快照（实测快照里的 `world.ts` 仍是旧的）。要刷新必须删掉
  `node_modules/.pnpm/<包名>@file+<包名>` 与 `node_modules/.pnpm-workspace-state-v1.json`、
  `.modules.yaml`，再 `pnpm install`。这次就是靠这个才让新代码进了快照。
  `docs/development.md` 早就写对了方向（"`file:` 依赖会被缓存，必要时先删掉 `extensions/node_modules`"，
  且给了 `--ignore-workspace` 这个更省事的命令），是第 12 轮的记录漏了这半句。

## 28. 版本三·第三轮:表情串台的真根因是队列永不结束,伴随通道值让表情看得出来（c3e6429）

- **要什么**：识别还有错误，大部分表情都错误地变成了唱歌，而且面部表情变化不明显。
- **根因（串台，比"没清零"深一层）**：pixi-live2d 的表情是 `CubismExpressionMotion`，时长
  -1，进了队列就**永不结束**——淡出因子在 `endTime < 0` 时被跳过，结束标记也永不置位——
  于是每挂过一张表情，它的参数就永远按权重 1 叠着。这份模型的唱歌/比心/葱共用
  `Param133-135` 各占一位：唱过歌之后再说「比心」，两个开关同时满足，显示成唱歌。
  库的 `setExpression()` 只写新表情的参数、不清零旧的，`resetExpression()` 只回模型默认态，
  都修不掉；**换挡那一刻清一次也没用**——下一帧旧动作又把值叠回来。上一会话先做的
  "挂新表情前清一次零"正是死在这里，本轮推翻重来。
- **根因（不明显）**：表情只写开关参数（脸红只写 `Param130`），切换的是贴图/部件，不改五官；
  五官由通道驱动，而情绪基线平时贴着中性，所以脸几乎不动。
- **改了什么（串台）**：表情参数改由页面**每帧**写。World 从模型自己的 exp3 文件推导每个
  表情写哪些参数、写多少（`modelExpressionTable`），帧里带 `expressionParams`（整组开关的
  清单）与 `expressionValues`（当前那张的值）；页面在 `writeFrame` 里先清零整组、再写当前
  值，位置在眼神跟随之后、参数定值之前——部署钉住的 `Param137=1`（水印关）仍最后赢。
  不再调用库的 `setExpression()`；`expressionToken` 随之退役（每帧都写，重放不再需要信号）。
  `Multiply` 混合的项不进值表：它乘的是已经被清零的 0，结果就是 0。
- **改了什么（不明显）**：`staging` 从"表情→片段名"升级为"表情→{片段, 通道值}"（旧写法
  仍认）：脸红→鼓嘴+眯眼抿笑、比心→雀跃+眉眼上扬、唱歌→摇摆+笑、温柔→前倾、圈圈→四顾。
  通道值走 `Performance.setStaging` 这条伴随基线：表情挂着就一直叠进合成，换挡即换掉、
  表情结束即清空。通道名过一道核对（`unknownStagingChannels`），包没抽象的通道挂载时告警。
- **被否掉的方案**：给兜底心情「平静」挂 QQ人（chibi）——那是画风不是情绪，
  `expressions.ts` 自己的规矩与测试都把它挡在外面；平静交给连续基线表达本来就说得通。
  （上一会话中断前正要做这条，因文件过期读失败而没做成。）
- **怎么验证**：扩展 96 项测试全绿（新增 5 项：parseStaging 新形状 2、伴随表齐全 1、坏包
  缺口列出 1、setStaging 叠加/替换/清空 1、推流帧带表情参数与换挡值 1；改写 2 项：
  "同一条指令只触发一次"改按"过期后不因再次命中而续期"断言，页面表情段改按每帧写参数
  断言——唱歌换比心，`Param134` 清零、`Param135` 写上）。`tsc --noEmit` 干净；page.test.ts
  的 8 个 `FakeNode.value` 类型错误与 `ParamMouthOpenY` 断言是第 24 轮就有的，未修。
  另用真实模型文件离线核对推导（scratch/check-exp3-table.cjs）：并集
  `Param125, Param130-137`，`heart => 133=0, 134=0, 135=1`——唱歌的开关被显式写零。
  快照按第 27 轮的办法重装过两次（第二次带上 README）。

## 29. 版本三·第四轮:串台的主语在措辞层——♪ 偷走了指令;表情加大加长（0e23a76）

- **要什么**：串台问题依旧存在，详细检测并修改；面部表情动作幅度大一点、时间长一点。
- **检测（运行时证据，不是猜）**：连上正在跑的 bot（今早 07:51 起）读推流帧——World 端
  是新代码（帧里带九个开关的清单与值），页面拿到的 app.js 也是新代码（每帧清零+写值），
  快照一致。再读今早的实测对话：你说「葱」，她回「葱——！……今天也要精神满满地过呀♪」。
- **根因**：指令按**表序**取第一个命中，而 sing 的词表里有「♪」、又排在 leek 之前——
  她的句尾习惯正是♪，于是几乎每条指令都被偷成唱歌。上一轮修的参数残留（表情开关叠着
  不散）是真实存在的另一层，但措辞层一直在**选错表情**。两层症状相同，修一层不够。
- **改了什么（串台）**：♪ 从 sing 的词表移除；唱歌仍由「唱歌/唱一小段/哼一小段/清唱/
  旋律/啦——」触发。vocab 里没有♪，词表片段那条路本来就干净。
- **改了什么（幅度）**：用真实 core 逐个核对九个开关——五个切不透明度（脸红最淡，贴图
  只有 0.58 不透明度）、两个是**顶点形变**（前倾、变小；只看不透明度会误判成"没效果"）、
  水印三条。表情开关全都不动五官，可见性要靠伴随通道值：整体加大（脸红 0.45→0.65、
  比心 0.7→0.9、唱歌 0.5→0.7、温柔 0.35→0.55、圈圈视线 0.6→0.9 等），并给 leek/chibi
  补上伴随——此前这两个被指令表点名的表情没有任何面部动作。
- **改了什么（时长）**：`expressionHoldMs` 默认 9000→15000（部署没覆盖它，重启即生效；
  控制台里仍可调）。
- **怎么验证**：扩展 97 项测试全绿（新增 1 项直接钉住复现句：「…过呀♪」不触发任何表情、
  「葱——！…」挂 leek、「温柔地陪着你」挂 lean、「唱一小段」挂 sing；"伴随表齐全"改为
  指令表里每个表情都必须有通道值）。`tsc --noEmit` 干净（8 个 FakeNode.value 是第 24 轮
  就有的）。真实 core 的开关核对脚本在 `cortico/scratch/check-exp-switches.mjs`。
- **操作员要做的一步**：重启 bot（`停止她.bat` → `启动她.bat`），并把形象页**强制刷新**
  （Ctrl+F5）——浏览器标签页若从旧会话一直开着，里面跑的还是旧 app.js。

## 30. 版本三·第五轮:眼神盯住光标、停满两秒才收回;拖动改面板开关（5ec5864）

- **要什么**：眼神随动改成——光标移动时一直盯向光标，光标停止 2 秒以上眼神才可以变；
  内部状态展开栏加一个「拖动」按钮，按下以后才能用鼠标拖动模型位置。
- **改了什么（眼神）**：光标一动就把注视目标设为它的位置，并起一个 2 秒的保持窗
  （`LOOK_HOLD_MS`）；`tick` 里发现停满 2 秒才把目标收回正前方——停着不动的那两秒里
  她一直看着它。指针离开窗口/失焦仍是立刻收回（原有行为）。跟踪速度不变（时间常数
  0.03s），移动中就是锁定的。
- **改了什么（拖动）**：「更多」面板的按钮行新增「拖动」开关（复位/记住旁边）。没开时
  覆层上的 pointerdown 直接忽略——点画面只是点；打开后光标变 grab、可以拖，再按一次
  关掉，正在拖的手就地停。顶部引导文案同步改。
- **怎么验证**：页面测试补四段——未开开关时覆层拖动不动她、开关的文案与按下态、
  关掉后画面点不动、眼神停 0.5s 仍在盯/满 2.3s 回正前方。扩展 97 项全绿；
  `tsc --noEmit` 干净（8 个 FakeNode.value 是第 24 轮就有的）。快照已重装。

## 31. 版本三·第六轮:视线游移收成小幅,跟随再快一倍（9e4d003）

- **要什么**：眼神随动不够灵敏；眼神随机转动时幅度小一些。
- **根因（两个诉求是同一处）**：待机视线游移的振幅是 2.4/1.2（EyeRightX/EyeRightY），被通道
  量程 [-1,1] 裁成**满幅 ±1**——眼睛整幅摆动；渲染端又把它**叠在指针跟随之上**，游移盖过
  跟随，鼠标移到哪儿都像没在看。所以"随机转动幅度大"和"跟随不灵敏"是同一个来源。
- **改了什么**：游移振幅收到 **0.6/0.3**——闲着时眼睛仍是活的，但不再扫满量程、不再盖过
  跟随；跟随时间常数 0.03s→**0.015s**（约 45ms 走到 95%，接近弹一下就到位）。
- **怎么验证**：扩展 98 项测试全绿（新增 1 项：20 秒采样内游移幅度不超 0.6/0.3、且确实
  在动——小幅不等于没有）；`tsc --noEmit` 干净（8 个 FakeNode.value 是第 24 轮就有的）；
  快照已重装。

## 32. 版本三·第七轮:摸头——闭眼脸红迎着手,数值按每日预算（301d98c）

- **要什么**：不拖动时按住鼠标左键抚摸模型头部——头随鼠标转动、脸红、闭眼、舒服的表情；
  增加心情、活力、羁绊，但每日靠摸头获得的数值有上限。
- **架构（三层各管各的）**：手势在**页面**（只有它有每帧的指针数据）；舒服态的表现与事件在
  **World**（表现归它，`pushEvent` 是它递进对话的通道）；情绪增量与每日预算在 **Persona**
  （情绪本来就归它，`personaState` 本来就持久化——预算跨重启成立）。
- **手势（页面）**：不开「拖动」时，左键按在头上即摸头。头部区域按模型此刻的尺寸估一个宽松
  椭圆（中心 = 画面中心 + 取景偏移——第一版把偏移当坐标，头被估到屏幕左上角，永远摸不到，
  测试抓住的）。每 2 秒心跳续期，松手/离窗/失焦结束；World 侧 5 秒没续上自收，页面崩了也
  不会一直挂着。摸头时头部随动从 6° 放大到 22°——头跟着手转。
- **表现（World）**：`POST /pat` 开关。开着时叠一层舒服态通道（闭眼 -1、嘴角 +0.2、头微迎），
  表情压成脸红（压过台词与心情）。顺手修了一处帧内不一致：`frame` 原来先合成通道再同步
  表情，换挡那一帧挂着新表情、动着旧表情的伴随通道；现在先同步再合成。
- **情绪（Persona）**：摸头开始那一刻，World 把「（摸了摸她的头）」作为**内部事件**递进对话
  （形象页只绑回环，是操作员自己的手），`flush` 立即投递——她会舒服地应一声；连着摸按
  5 秒冷却合并成一次。Persona 在 `onDelivery` 按**事件类型**识别（不玩文本匹配），不走词表，
  按当日预算给 心情+0.05 / 活力+0.03 / 羁绊+0.06；预算以心情增量计
  （`emotion.patDailyGain`，默认 0.25，控制台可调，写 0 = 摸头只舒服不给数值），余额不足按
  比例，日期一翻重新开始，随 personaState 持久化。
- **怎么验证**：扩展 102 项 + `tests/miku` emotion 24 项全绿（新增：`patEffect` 五项——一轮
  增量、预算用尽、按比例、跨天翻篇、预算为零；World 三项——舒服态帧、事件与冷却、坏请求
  400；`setPat` 一项；页面手势一段——按头发出心跳、头部 22°、松手回 6°、打在身上不触发）。
  `tsc --noEmit` 干净（8 个 FakeNode.value 是第 24 轮就有的）。快照已重装。
- **操作员要做的一步**：重启 bot + 形象页 Ctrl+F5。

## 33. 版本三·第八轮:摸头识别区上移到头顶;眼睛偏移这个老 bug 一起拔了（6799567）

- **要什么**：把摸头识别区改到头顶；让她在摸头时闭眼。
- **识别区**：原来以模型中心上方 0.38h 为心、纵向半轴 0.16h——一直盖到胸口。用真实模型
  标定几何（`scratch/check-pat-geometry.mjs`）：脸颊（脸红贴图 ArtMesh31）在模型顶部往下
  0.16 高度处。识别区改到**头顶一圈**：中心 0.43h、纵向半轴 0.08h、横向 0.18w——摸头顶
  和额头有效，摸脸和身上不触发。
- **闭眼没生效的根因（比摸头老得多的 bug）**：部署给眼睛通道配了 `paramOffset +1`（本意是
  对齐「0=平常睁眼」与参数「1=睁眼」的约定差）。但渲染端对**眨眼参数是加法写入**——加法
  约定里 0 本来就是「不动眨眼」。实测模型：`ParamEyeLOpen/ROpen` 在眨眼组里、量程 0..1、
  默认 1。于是 +1 偏移把参数钉死在 `blink+1`→裁到 1：**她一直不眨眼**；眯眼通道（脸红
  -0.5）被抵消成 +0.5；摸头的闭眼（-1）被抵消成 0——三件事全坏在这一个偏移上。
- **改了什么**：删掉部署里眼睛的 `paramOffset`（部署配置，不在 git；`deployments/miku/
  config.json`）。眨眼恢复、情绪垂眼/脸红眯眼/摸头闭眼全部按通道值生效。偏移机制本身保留
  （绝对写入的通道仍可用）；world 测试里「不加偏移就会写成全闭」的注释已被证伪，改成陈述
  规矩：**眨眼类通道不要配偏移**。
- **怎么验证**：扩展 102 项 + `tests/miku` emotion 24 项全绿（页面测试的头顶坐标随识别区
  更新，打在身上的用例仍不触发）；`tsc --noEmit` 干净（8 个 FakeNode.value 是第 24 轮就有
  的）。快照已重装。
- **操作员要做的一步**：重启 bot + 形象页 Ctrl+F5（部署配置改了，必须重启才读得到）。

## 34. 版本三·第九轮:摸头识别区锚定头部网格,跟着头与取景走（f1920a7 + 9ec5173）

- **要什么**：摸头的识别区与模型严重偏差——她的头明明在画面中间偏右,识别区却落在屏幕
  中心一带,摸她的头没反应;模型缩放、拖动取景之后,识别区也不跟着走。
- **根因（拿真实模型量出来的）**：识别区按「画布中心 + 画布尺寸的分位」估头。第 33 轮的
  分位（中心上移 0.43h、半轴 0.08h/0.18w）是按**内容并集**（全体网格顶点的范围）标定的,
  而页面把它套在了**画布**上。这份模型画布 3500×8888 像素、内容并集宽 4414 像素——纵向
  两者几乎重合（差 3 像素）,横向却差了 1153 像素（画布宽的 33%）:内容中心在画布中心右边,
  识别区却以画布中心为心,于是整个偏在头左边,只擦到左脸颊的边,头顶永远摸不到。取景
  （缩放/位移）倒是跟着 `model.width` 与偏移走了,但估的基准本身不在头上,跟得再准也没用。
- **改了什么**：识别区不再估,改**锚定**。素材包新增 `pat.json`,点名头部网格（ArtMesh207,
  盖住头顶的前发）;World 把它经 `/pack/pat.json` 下发（包里没点名时启动即告警）。页面把
  点击换算回画布坐标（`toModelPosition`,取景与缩放在这一步消化）,对该网格**此刻**的顶点
  范围做椭圆判定——分位按真实模型重新标定（画布 Y 向下、从网格上沿量:中心 0.29 网格高,
  半轴 0.36 网格宽 / 0.42 网格高;第一版把纵向分位写成了从下沿量,区域整个扣在嘴上,
  真实 moc 探针抓住的）。网格跟着头动——转头、身体摆动、取景缩放拖动、窗口变化,识别区
  都在她头上。
- **怎么验证**：扩展 103 项,除 README「已知未完成」里那条口型断言外全绿（摸头一段重写:
  按头触发、打肩上不触发、**网格挪动后原点摸空而头的新位置触发**、**取景拖走后原屏幕点
  摸空而头的新屏幕位置触发**;新增 `/pack/pat.json` 下发一项）;`tsc --noEmit` 干净——
  顺手给页面测试的 FakeNode 补上 `value` 字段,第 24 轮起挂着的 8 个类型错误清零;
  `tests/miku` 71 项全绿。真实模型核对（`scratch/check-pat-region.mjs`,三种姿态）:
  头顶/额头/脸颊上沿触发,嘴与肩不触发;旧公式的识别区中心距头顶 1153 像素,头顶不在其中。
- **操作员要做的一步**：重启 bot + 形象页 Ctrl+F5（素材包与页面都换了）。

## 35. 版本三·第十轮:形象页换装——B 方案浅色发调,白玻璃流行牌（d8b8c2d）

- **要什么**：形象页的 UI 换装。操作员在三个参考方向里选了 **B（白底流行）**，并要求浅色
  背景、主要参考色取自《モニタリング》Remix 封面的发色（淡蓝、淡青）。
- **取色（MV 画面抓不到，拿官方封面定量采）**：youtube 域名被本机加速工具劫持，改从 Apple
  音乐库拿到《モニタリング》三张官方封面高清图，逐像素统计主色（警示黄/青/粉/近黑/白/藏蓝），
  出了 A 監視室、B 白底流行、C 深夜监控三个方向的样张（`界面参考/`，含取色来源与对比页，
  因内有官方封面图不入库）。选定 B 后对 Remix 封面单独做**发色采样**（只取高亮度、青蓝
  hue 的像素簇，占画面 30.6%）：淡蓝 `rgb(223,230,240)`、淡蓝紫 `rgb(198,215,241)`、
  中钢蓝 `rgb(104,146,196)`，高光近白。
- **改了什么**：只动质感层——`web/index.html` 的样式块整体重写，`:root` 换成发调色板。
  组件语言是**白玻璃流行牌**：白底 + 2px 墨线 + 硬投影，悬停落青、按压把投影按回去。
  颜色按角色锁死：青 `#0FB0BF` = 交互与她的数据条，钢蓝 `#4A6FA5` = 标签与链接，
  黄 `#FAFE03` 只做 LIVE 徽标（连上后由 `#conn.on::before` 挂出，不占 DOM）；粉按
  「主要为淡蓝淡青」退场。背景从近黑换成发色的淡青到淡蓝渐变 + 网点纸感（网点铺在
  背景层，在她身后不压在她身上）；字幕从无边框扁条换成白底墨边；心情值做成青底小圆牌。
  功能代码只动一行：`app.js` 里连接徽章连上/断开都保持 `glass` 底——原来
  `className='on'` 会把玻璃底整个替掉，连上后徽章一直是裸文字。
- **怎么验证**：扩展 103 项，除已知那条口型断言外全绿；`tsc --noEmit` 干净。样张
  `mock-b2.png`（无头 Edge 渲染）逐像素复验：淡青 `rgb(214,231,237)` 与发色淡蓝
  `rgb(218,228,240)` 为底、白面板、墨线俱在，与设计意图一致。对比度：墨字对白底
  ≈ 19:1，墨字对青 ≈ 7:1，占位符 `#6B7288` 对白底 ≈ 4.8:1，全过 AA。
- **操作员要做的一步**：`启动她.bat` + 形象页 Ctrl+F5。

## 36. 版本三·第十一轮:旧标签页自刷新;口型死分支一起拔了（17ee349 / 2b39941）

- **要什么**：换过页面之后，操作员手里开着的旧标签页不会自己变新——每一轮的收尾都要写
  「形象页 Ctrl+F5」，忘了就以为改动没生效。本轮让页面自己知道自己是旧的。
- **改了什么（自刷新）**：入口注入页面版本（`index.html` 与 `app.js` 的大小和修改时间的
  sha1 前 8 位，`sendPage` 注入 `window.__DSH_PAGE_VER__` 并缀在标签页标题上），每帧随
  `/state` 下发（放在帧尾，老页面的解析不受影响）；页面发现帧里的版本与入口注入的不符就
  `location.reload()`。文件没动过的重启版本不变，连着的页面不会白刷；版本缀在标题上，
  这份页面是哪一版一眼看得见。
- **口型（顺手拔掉挂了 12 轮的已知问题）**：第 24 轮起挂着的 `page.test.ts` 口型断言，
  根因 `记录/README.md` 里早就写对了——`tick` 把 `MouthOpen` 摘出平滑（对的一半：片段
  的包络自带起落，平滑会把「张嘴」的峰值磨掉），而 `writeFrame` 只遍历 `current` 里的
  通道，`MouthOpen` 永远不在其中：口型参数**从未写过**，说话振荡与片段张嘴都到不了模型，
  第 17 轮的 `max(振荡, 通道值)` 改在一条死分支上。现在口型单独写：每帧直取帧值，与说话
  振荡取大的那个；`writeParam` 抽出来，通道与口型共用一套"眨眼组加法、其余定值"的写法。
- **这一修的连带事实**：口型断言排在页面测试中段，它挂着的时候，同一测试里排在它后面的
  段落（取景 / 摸头 / 眼神 / 对话 / 表情换挡）不会执行。本轮修掉后整段第一次真正跑过，
  全部通过——那些段落本身没有写错。
- **怎么验证**：扩展 104 项全绿（world 新增 1 项：入口注入的版本与帧里带的一致；页面测试
  内补三段：帧里没带版本不刷、与注入的一致不刷、不符刷一次）；`tsc --noEmit` 干净；
  `pnpm check:extension` 通过。快照按第 27 轮的办法重装（删 `.pnpm` 里该包的缓存条目与
  workspace 状态再 install——注意命令要在 `extensions/` 目录下跑，在包目录里跑它装的是
  包自己的开发依赖），并核对快照内 `app.js` / `world.ts` 都是新代码。
- **操作员要做的一步**：重启 bot（`停止她.bat` → `启动她.bat`）。形象页**不用**再
  Ctrl+F5——旧标签页在重连后自己刷新成新页面，标题上的版本号会变。

---

## 当前状态

- 能跑的是版本三——版本二那套 cortico 实现从第 26 轮起的继续，入口不变：`D:\二面\启动她.bat` /
  `停止她.bat`，控制台 `http://127.0.0.1:18790/`，形象页 `http://127.0.0.1:18795/`。
- 2026-09-19 起，`D:\二面` 整体是一个 git 仓库，推送到 GitHub（`lsq01151017/miku-agent`，公开）。
  cortico 的 45 个提交历史以合并方式并入 `cortico/` 子目录；cortico 内部照旧独立提交，
  外层仓库按文件同步后再 push。密钥（`my-agent/.env`）不入库，克隆者按 `my-agent/.env.example` 自备。
- 未完成与已知限制见 `README.md` 的"已知未完成"。
- 题目各层级的落点与实测证据见 `验收证据与取舍.md`。
